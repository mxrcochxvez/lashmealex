# Repo Rules

- Use atomic commit scopes and keep each commit as small and clean as reasonably possible.
- Use Karma-style commit messages for all git commits.
- Prefer separate commits for workspace setup, backend scaffolding, storefront UI, and follow-up fixes instead of bundling unrelated changes together.
